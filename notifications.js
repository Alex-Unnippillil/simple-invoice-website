function sendNotification(recipient, message) {
  // Placeholder for real notification logic (email, SMS, etc.)
  console.log(`Notify ${recipient}: ${message}`);
}

module.exports = { sendNotification };
